<Window x:Class="SzakdolgozatErtekeloWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SzakdolgozatErtekeloWPF.Controls"
        mc:Ignorable="d"
        Title="Szakdolgozati Bírálati Lap"
        Height="950"
        Width="1200"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FB">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Görgetési terület – minden szekció -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">

            <StackPanel>

                <controls:HeaderControl x:Name="Header"/>

                <controls:SwitchPanelControl x:Name="SwitchPanel"/>

                <controls:StudentDataControl x:Name="StudentData"/>

                <controls:EvaluationSection x:Name="Evaluation"/>

                <controls:SummaryControl x:Name="Summary"/>

            </StackPanel>

        </ScrollViewer>

        <!-- Footer – alul ragad, mindig látható -->
        <controls:FooterControl x:Name="Footer" Grid.Row="1"/>

    </Grid>

</Window>
